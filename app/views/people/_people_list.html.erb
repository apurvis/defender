<table>
   <tr>
      <th>Most Recent Role</th>
      <th>Name</th>
      <th>Edit</th>
      <th>Delete</th>
   </tr>

   <% @people.each do |p| %>
      <tr>
         <td><%= p.most_recent_role %></td>
         <td><%= link_to(p.name, p) %></td>
         <td><%= link_to 'Edit', edit_person_path(p) %></td>
         <td><%= link_to 'Delete', p, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
   <% end %>
</table>
