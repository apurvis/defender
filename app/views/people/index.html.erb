<h1><%= @class_name.pluralize %> (<%= link_to "Add New #{@class_name}", self.send("new_#{@class_name.downcase}_path".to_sym) %>)</h1>

<%= render partial: 'people_list', locals: { people: @people } %>
